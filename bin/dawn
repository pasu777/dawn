#!/usr/bin/env node
var dawn = require('../libs/dawn.js');
var commander = require('commander');
// 
// var app = dawn().listen(80);
// var asset = dawn().listen(90);

 // use config file and worker router 